/** Primary brand green — hero, header, CTAs, accents (matches site teal) */
export const BRAND_GREEN = "#009689";

/** Full-width section band — use inline so the gradient always paints (avoids missing utility/CSS order issues) */
export const BRAND_SURFACE_GRADIENT = `linear-gradient(145deg, ${BRAND_GREEN} 0%, #007a70 45%, #065f56 100%)`;
